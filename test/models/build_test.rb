require "minitest_helper"

class BuildTest < MiniTest::Rails::Model

  it 'must belong to a project' do
    assert true
  end

  it 'must have a last commit' do
    assert true
  end

  it 'must have a last commiter' do
    assert true
  end

  it 'must have a status which is true or false' do
    assert true
  end

end
