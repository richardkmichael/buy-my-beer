-# TODO: Q: Here I must use the class variable?  There is no 'message'.
%h1= @message

.projects
  %h2 Projects
  %h2.button= link_to('Create a New Project', new_projects_path)
  = render @projects
  = 'You have no projects!' if @projects.empty?
